export type ProgressBarOptions = {
    /** 总数 */
    total?: number;

    /** 是否显示百分比 */
    percentage?: boolean;

    /** 是否显示计数 */
    count?: boolean;

    /** 是否显示动态进度条图像 */
    picture?: boolean;

    /** 是否隐藏光标 */
    hiddenCursor?: boolean;
};
