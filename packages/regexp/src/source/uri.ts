/**
 * 验证 `URI` 连接的正则字符串
 *
 * 在 `URI` 中，除字母、数字和字符 `$-_.+!*'()` 外的其它字符应优先使用字符的编码(`%0X`)。
 *
 * ```javascript
 * const reg = /^(https?|ftp|file):\/\/[- ?!#$@&|^+/*~%=.,:;(){}<>\\`'"[\]\w]+[+&@#/%=~|\w]$/g;
 * ```
 */
const uri =
    '(https?|ftp|file):\\/\\/[- ?!#$@&|^+/*~%=.,:;(){}<>\\\\`\'"[\\]\\w]+[+&@#/%=~|\\w]';

export default uri;
