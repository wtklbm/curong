import { isSpaceString } from '../src';

describe('@curong/string/isSpaceString', () => {
    test('测试1', () => {
        expect(isSpaceString('')).toBe(false);
    });

    test('测试2', () => {
    });
});
